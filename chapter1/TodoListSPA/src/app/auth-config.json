{
  "credentials": {
    "clientId": "Enter the client ID (aka 'Application ID')",
    "tenantId": "Enter the tenant ID"
  },
  "configuration": {
    "redirectUri": "http://localhost:4200",
    "postLogoutRedirectUri": "http://localhost:4200"
  },
  "resources": {
    "todoListApi": {
      "resourceUri": "Enter the endpoint for TodoList web API, e.g. https://localhost:44351/api/todolist",
      "resourceScopes": [ "Enter the API scopes as declared in the app registration 'Expose an API' blade, e.g. api://{clientId}/access_as_user" ]
    }
  },
  "roles": {
    "TaskAdmin": "TaskAdmin",
    "TaskUser": "TaskUser"
  }
}